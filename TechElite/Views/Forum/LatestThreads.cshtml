@using TechElite.Models
@model ForumViewModel
@{
	ViewData["Title"] = "Latest threads";
}

<head>
    <link rel="stylesheet" href="~/css/forum.css" />
    <link rel="stylesheet" href="~/css/thread.css" />
</head>

<main>
    <div class="forum-container">
        <div class="filter-icons">
            <div class="dropdown">
                <button>
                    <img src="~/img/Icons/filter_blck.svg" alt="icon">
                </button>
                <div class="dropdown-content">
                    @foreach (var c in Model.ForumCategories)
                    {
                        <a asp-controller="Forum" asp-action="FilterThreads" asp-route-id="@c.CategoryId">@c.CategoryName</a>
                    }
                </div>
            </div>
        </div>
    <article>
        <section>
            <h4>Alla trådar</h4>
            <div>
                @foreach (var t in Model.ForumThreads)
                {
                    <a class="new-thread" asp-controller="Forum" asp-action="Thread" asp-route-id="@t.ThreadId">@t.ThreadTitle</a>
                }
            </div>
        </section>
    </article>
    </div>
</main>